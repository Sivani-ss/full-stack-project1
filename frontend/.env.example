# Optional locally — only needed if you test a production build against a remote API.
# On Vercel, add the same as an Environment Variable (Production + Preview).
# Example: https://your-service.onrender.com  (no trailing slash)
# REACT_APP_API_URL=https://your-service.onrender.com
